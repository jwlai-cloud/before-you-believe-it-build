# Add these in Vercel Project Settings → Environment Variables. Never expose them in the browser.
OPENAI_API_KEY=replace-with-your-server-side-key
OPENAI_MODEL=gpt-5.6
# Required to unlock the optional live demo. Use a unique, high-entropy value (16+ characters).
DEMO_ACCESS_TOKEN=replace-with-a-unique-judge-access-code
